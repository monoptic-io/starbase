# build output
/_site/
/site/
demo/_site/
**/.starbase-cache.json

# binaries
/starbase
/sitegen
/dist/

# vendored third-party assets — downloaded at build time with --vendor, never committed
internal/assets/static/katex/
**/static/katex/
.starbase/

# OS
.DS_Store

# local evidence cache (also kept in the user cache dir)
**/.starbase-evidence.json
